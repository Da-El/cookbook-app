DATABASE_URL=postgres://postgres:devpassword@localhost:5432/cookbook
PORT=8090

# Optional. Without RESEND_API_KEY, transactional email (password reset)
# logs to stdout instead of sending - that's a real fallback, not a stub,
# so these are fine to leave unset in local dev.
# RESEND_API_KEY=
# EMAIL_FROM=Cookbook <onboarding@resend.dev>
# APP_URL=https://cookbook-app-q8ga.onrender.com
